spring:
  application:
    name: templa
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: password
  datasource:
    url: jdbc:postgresql://localhost:5432/templa
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    jwt:
      secret: secret
      expires-in: 360
      